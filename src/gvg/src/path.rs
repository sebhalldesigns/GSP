// Path is the fundamental entity for rendering. 
// Every shape gets transformed into a path, but users can also submit a custom path.

// Approximate rendering pipeline:
// Init state machine:
// seed threads